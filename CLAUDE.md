# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

MBTI 공부법 연구소 — HTML 여러 페이지로 구성된 콘텐츠 사이트.

## Design

- 크림 배경 + 보라 포인트 컬러
- Pretendard 폰트
- 모바일 반응형

## Rules

- 서버·API·키는 절대 사용하지 않는다 (정적 파일만)
- 파일이 300줄을 넘으면 분리를 먼저 제안할 것

## Project status

Directory currently has no source files beyond this CLAUDE.md. It sits inside a larger monorepo at `/Users/hs/Desktop/vibe`, alongside sibling project folders (e.g. `3days`, `city_tour_food`, `fine_dust`, `news`, `project_HUB`, `receipt`, `review`, `walk_app`, `water`, `weather`).

Once code is added here, update this file with:

- Build, lint, and test commands (including how to run a single test)
- The high-level architecture and structure of whatever app/tool is built
